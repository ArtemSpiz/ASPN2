<div id="popup-modal" class="modal hidden">
  <div class="modal-content">
    <h2>Application Form</h2>
    <p>
      To join the waitlist and be among the first to experience ASPN Health’s personalized performance approach, please
      fill out the form below.
    </p>
    <form method="POST" action="/contact" id="lead-form">
      <!-- Name Field -->
      <label for="name">Your Name</label>
      <input
        type="text"
        id="name"
        name="name"
        placeholder="Input your name"
        required
      >

      <!-- Email Field -->
      <label for="email">Email</label>
      <input
        type="email"
        id="email"
        name="email"
        placeholder="example@domain.com"
        required
      >

      <!-- Buttons -->
      <div class="form-actions">
        <button type="button" id="close-modal" class="close-btn">Cancel</button>
        <button type="submit" class="submit-btn">Submit</button>
      </div>
    </form>
  </div>
</div>

<style>
        .modal {
          position: fixed;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          background-color: rgba(0, 0, 0, 0.7);
          display: flex;
          justify-content: center;
          align-items: center;
          z-index: 1000;
          opacity: 0;
          pointer-events: none;
          transition: opacity 0.3s ease;
        }

        .modal.show {
          opacity: 1;
          pointer-events: auto;
        }

        .modal-content {
          background: #ffffff;
          padding: 60px;
          border-radius: 16px;
          text-align: center;
          width: 90%;
          max-width: 800px;
          box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
          font-family: "Host Grotesk";
        }

        .modal-content h2 {
          color: #0A0601;

          text-align: center;
          font-size: 48px;
          font-style: normal;
          font-weight: 700;
          line-height: 120%;
        }

        .modal-content p {
          color: var(--Neutral-600, #5D5D5D);
          text-align: center;
          font-size: 18px;
          font-style: normal;
          font-weight: 400;
          line-height: 140%;
          margin-top: 16px;
          margin-bottom: 32px;
        }

        .modal-content label {
          color: var(--Neutral-950, #131313);
          font-family: "Host Grotesk";
          font-size: 16px;
          font-style: normal;
          font-weight: 400;
          line-height: 120%;
          float: left;
           margin: 15px 0;
        }

        .modal-content input {
          width: 100%;
          border: 1px solid #131313;
          border-radius: 6px;
          font-size: 16px;
          display: flex;
          height: 48px;
          padding: 8px 12px;
          align-items: center;
          gap: 9.03px;
          align-self: stretch;
          border-radius: 12px;
          background: linear-gradient(109deg, rgba(255, 255, 255, 0.10) -29.02%, rgba(255, 255, 255, 0.05) 136.41%);
          backdrop-filter: blur(6.772277355194092px);
        }

         .modal-content input:focus-visible {
           outline: none;
           outline-offset: none;
           box-shadow: none;
         }

        .form-actions {
          display: flex;
          justify-content: space-between;
          align-items: center;
          margin-top: 32px;
          gap: 12px;
        }

        .close-btn {
          background: none;
          border: 1px solid #555;
          padding: 16px 32px;
          border-radius: 6px;
          cursor: pointer;
          font-size: 14px;
          width: 50%;
          border-radius: 99px;
          border: 1px solid var(--Neutral-950, #131313);
          box-shadow: 0px 1px 2px 0px rgba(16, 24, 40, 0.05);
        }

        .submit-btn {
          color: white;
          border: none;
          padding: 16px 32px;
          border-radius: 6px;
          cursor: pointer;
          font-size: 14px;
          width: 50%;
          border-radius: 99px;
          background: var(--Primary-500, #F08A35);
          /* Shadow/xs */
          box-shadow: 0px 1px 2px 0px rgba(16, 24, 40, 0.05);
          color: black;
        }

        .submit-btn:hover {
          background: #e06b00;
        }

        @media (max-width: 700px) {
        .modal-content {
          padding: 24px;
        }

        .modal-content h2 {
          font-size: 26px;
        }

        .modal-content p {
          font-size: 12px;
          margin-bottom: 32px;
        }

        .modal-content label {
        }

        .modal-content input {
         padding: 8px 12px;
        }

         .modal-content input:focus-visible {
         }

        .form-actions {
          display: flex;
         flex-direction: column-reverse;
        }

        .close-btn {
          width: 100%;
        }

        .submit-btn {
          width: 100%;
        }
  }
</style>
